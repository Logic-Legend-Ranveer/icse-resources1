/**
 * ─────────────────────────────────────────────────────────────────────────
 * Not used — this project is on Tailwind CSS v4.
 * ─────────────────────────────────────────────────────────────────────────
 * v4's `@tailwindcss/vite` plugin does not read this file by default (that
 * was v3's JS-config model). All theme customization — colors, shadows,
 * keyframes/animations — now lives directly in `src/index.css` inside the
 * `@theme { ... }` block, using CSS custom properties in the `--color-*`,
 * `--shadow-*`, and `--animate-*` namespaces.
 *
 * You can safely delete this file. It's kept only so nothing 404s if
 * something still references it, and as a pointer for anyone who goes
 * looking for the "config" out of habit.
 */
export {};
